-- Development seed ONLY. Never run against production.
-- Passwords for local Auth users are set via supabase/seed helpers below.

-- Default landing already inserted in migration.
-- Feature flags already inserted.

-- Local auth users are created by `supabase/seed-auth.sql` when using the
-- helper script, or manually:
--   email: owner@ario.local   password: ArioOwner!123
--   email: ali@ario.local     password: ArioDemo!123
--   email: sara@ario.local    password: ArioDemo!123
--   email: nima@ario.local    password: ArioDemo!123

select 1;
